\begin{enumerate}
    \item Выбраны значения $p=\VAR{"%x" % rsa.gen.p}$ и $q=\VAR{"%x" % rsa.gen.q}$;
    \item $n = p \cdot q = \VAR{"%x" % rsa.gen.n}$;
    \item $\phi(n) = (p-1)(q-1) = \VAR{"%x" % rsa.gen.phi}$;
    \item Выбирано $e = \VAR{"%x" % rsa.gen.e}$;
    \item Из уравнения $d\cdot e\equiv 1{\pmod {\varphi (n)}} $ найдено $d = \VAR{"%x" % rsa.gen.d}$;
    \item Пара $\left\{\VAR{"%x" % rsa.gen.e},\VAR{"%x" % rsa.gen.n}\right\}$ \-- открытый ключ RSA;
    \item Пара $\left\{\VAR{"%x" % rsa.gen.d},\VAR{"%x" % rsa.gen.n}\right\}$ \-- закрытый ключ RSA.
\end{enumerate}